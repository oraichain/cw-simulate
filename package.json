{
  "name": "root",
  "private": "true",
  "license": "MIT",
  "scripts": {
    "postinstall": "patch-package",
    "test": "yarn build && jest",
    "clean": "lerna clean --yes && lerna exec -- rimraf build/ dist/ cache/",
    "build": "lerna run build --scope @oraichain/cw-simulate --scope @oraichain/cosmwasm-vm-js --concurrency 1",
    "build-tsc": "tsc -p"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=18.18.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.5",
    "esbuild-runner": "^2.2.2",
    "@types/jest": "^29.5.2",
    "@types/node": "^22.9.0",
    "dotenv": "^16.3.1",
    "jest": "^29.5.0",
    "lerna": "^7.1.5",
    "nx": "16.10.0",
    "patch-package": "^7.0.0",
    "tsx": "^4.7.2",
    "typescript": "^5.3.2"
  }
}
